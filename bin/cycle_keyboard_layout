#!/usr/bin/env bash


if setxkbmap -query | grep -q -e 'layout:\s\+es'; then
  echo us
  setxkbmap -layout us -option ctrl:nocaps -variant altgr-intl
else
  echo es
  setxkbmap -layout es -option ctrl:nocaps
fi

# Restart py3status bar
killall -USR1 py3status
